name: function_filter
description: A Dart library for function filtering utilities, providing tools for debouncing and throttling function executions based on time intervals.
version: 2.1.3
homepage: https://github.com/Mopriestt/function_filter

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.17.0"

dev_dependencies:
  test: ^1.24.9
